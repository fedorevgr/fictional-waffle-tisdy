#include "include/Table.h"
#include "include/Car.h"

int
main(void)
{
    tableFindCars(0);
    tableAddCar(NULL_CAR);
    tableFindCars(0);
    return 0;
}

ExitCode
perform(int p)
{
    switch (p)
    {
        case 1:break;
        case 2: break;
        case 3: break;
    }
}

void
pool(void)
{
    int p = 0;

    do
    {

    }
    while (p)

}
