#ifndef BINARYTREES_SRC_SHUFFLE_H
#define BINARYTREES_SRC_SHUFFLE_H

#include <time.h>
#include <stdlib.h>

int
shuffle(void *array, size_t size, size_t element_size, double shuffleFactor);

#endif //BINARYTREES_SRC_SHUFFLE_H
